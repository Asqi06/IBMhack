<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- v1.2: opted-in live-call & QR shield — see disclosure in MainActivity -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <!-- dataSync is the type the bubble runs under BEFORE screen-capture consent exists.
         Android 14+ throws SecurityException if we claim mediaProjection without consent. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Required on API 33+ or the foreground-service notification (and "Scan now") never appears -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- SMS Retriever API — privacy-safe, delivers SMS content that matches app hash;
         no READ_SMS needed. For scam-checking approved by brief Section 2. -->
    <uses-permission android:name="android.permission.SEND_RESPONSIVE_SMS" />
    <!-- v1.2 live-call shield: optional, user-granted, on-device STT only text sent -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.USE_SIP" tools:node="remove" />
    <!-- Caller-ID pre-check (CallScreeningService needs no extra permission, but telecom role helps) -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" tools:node="remove" />
    <!-- QR / UPI scanner -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Vibration for scam alert (3 buzzes) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.ScamShield"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.ScamShield">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".FloatingWidgetService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|dataSync" />

        <!-- v1.2 Live Call Shield — on-device STT, only text sent to /analyze-call -->
        <service
            android:name=".CallShieldService"
            android:exported="false"
            android:foregroundServiceType="microphone|phoneCall" />

        <!-- v1.2 Caller-ID pre-check — shows ledger risk before you answer -->
        <service
            android:name=".ScamCallScreeningService"
            android:permission="android.permission.BIND_SCREENING_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <service
            android:name=".WhatsAppListenerService"
            android:label="ScamShield WhatsApp Guard"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".SmsReceiver"
            android:exported="true">
            <intent-filter android:priority="100">
                <action android:name="android.SMS_RETRIEVED_ACTION" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".LinkInterceptorActivity"
            android:exported="true"
            android:theme="@style/Theme.ScamShield">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>
        </activity>

        <activity
            android:name=".QrScannerActivity"
            android:exported="false"
            android:theme="@style/Theme.ScamShield" />

        <!-- Home-screen App Widget (companion to floating bubble MVP) -->
        <receiver
            android:name=".ScamShieldWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/scamshield_widget_info" />
        </receiver>
    </application>

</manifest>
